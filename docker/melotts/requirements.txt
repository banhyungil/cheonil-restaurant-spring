# MeloTTS 본체 + 한국어 phoneme 의존성은 setup.py 에서 transitively 설치됨.
# torch 는 CPU 전용으로 명시해 이미지 크기/메모리 절약.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
torchaudio==2.2.2

git+https://github.com/myshell-ai/MeloTTS.git@main

# HTTP 서버
fastapi==0.115.0
uvicorn[standard]==0.30.6

# WAV → MP3 변환용 (ffmpeg wrapper)
pydub==0.25.1
